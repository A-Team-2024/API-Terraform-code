This Terraform code deploy the following services: 

API gateway,
AWS Lambda,
Dynamo DB,
IAM roles,
CORS policy

The terraform code is written in modules form. Run the script from the project folder.
